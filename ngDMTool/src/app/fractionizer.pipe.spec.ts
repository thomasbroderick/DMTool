import { FractionizerPipe } from './fractionizer.pipe';

describe('FractionizerPipe', () => {
  it('create an instance', () => {
    const pipe = new FractionizerPipe();
    expect(pipe).toBeTruthy();
  });
});
